# DEV Database (requires SSH tunnel: ssh -f -N -L 5433:localhost:5434 root@165.232.86.131)
DEV_POSTGRES_DSN=postgresql://airflow:airflow@localhost:5433/kansofy_data

# Anthropic API (for expensive Sonnet tests)
ANTHROPIC_API_KEY=sk-ant-...

# HubSpot (for integration tests)
HUBSPOT_ACCESS_TOKEN=pat-...

# Test Configuration
RUN_EXPENSIVE_TESTS=false
